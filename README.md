# FirstIn

FirstIn polls company career pages for newly posted software engineering roles and notifies you before anyone else. V1 supports Greenhouse boards only.

**Build & run:** `just build` then `just run`. Config: `config.yaml` (see CLAUDE.md for layout).
